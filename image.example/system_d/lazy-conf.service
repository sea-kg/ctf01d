[Unit]
Description=Lazy Conf
After=syslog.target
After=network.target

[Service]
WorkingDirectory = /usr/share/hackwars/lazy-conf
User=root
Group=root
ExecStart=/bin/sh -c 'python2 service.py'

TimeoutSec=30
Restart=always

[Install]
WantedBy=multi-user.target
Alias=lazy-conf
